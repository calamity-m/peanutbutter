# These files are embedded into the binary verbatim (see build.rs). Pin them to
# LF so a CRLF checkout on some platforms cannot make the embed — and the
# source-vs-embed tests — diverge.
docs/SNIPPET_SYNTAX.md text eol=lf
examples/config.toml text eol=lf
examples/starter_snippets.md text eol=lf
